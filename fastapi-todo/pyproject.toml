[project]
name = "fastapi-todo"
version = "0.1.0"
description = "FastAPI todo backend conforming to the todobackend.com spec"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk"
]
