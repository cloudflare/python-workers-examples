[project]
name = "python-chatroom"
version = "0.1.0"
description = "Python WebSocket chatroom example"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk"
]
