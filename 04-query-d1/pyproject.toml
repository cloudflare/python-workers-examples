[project]
name = "python-d1"
version = "0.1.0"
description = "Python d1 example"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "webtypy>=0.1.7",
]

[dependency-groups]
dev = [
    "workers-py"
]
