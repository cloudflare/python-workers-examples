[project]
name = "flask-notes-worker"
version = "0.1.0"
description = "Todo notes app on a Python Cloudflare Worker using Flask and D1"
requires-python = ">=3.13"
dependencies = [
    "flask",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
