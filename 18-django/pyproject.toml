[project]
name = "django-worker"
version = "0.1.0"
description = "Simple Django example for Python Workers"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
