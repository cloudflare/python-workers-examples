[project]
name = "python-container-do"
version = "0.1.0"
description = "Python container-backed Durable Objects example"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk"
]
