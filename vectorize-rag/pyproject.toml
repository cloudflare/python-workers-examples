[project]
name = "python-vectorize-rag"
version = "0.1.0"
description = "Python RAG example with Workers AI and Vectorize"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
